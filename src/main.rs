fn main() {
    // Milestone 1 (skeleton: hook subcommands, daemon, socket protocol) starts here.
    eprintln!("cfetch {}: nothing implemented yet", env!("CARGO_PKG_VERSION"));
    std::process::exit(1);
}
